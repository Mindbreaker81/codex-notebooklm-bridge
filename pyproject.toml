[project]
name = "codex-notebooklm-bridge"
version = "0.5.0"
description = "Codex skill that queries Google NotebookLM through the user's authenticated Chrome session."
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
